// Moved to src/kernel/bridge/http.js (round four, the kernel). This shim keeps the old import path
// working until K2 rewrites the shell against `ose:kernel`.
export * from '../kernel/bridge/http.js';
