// Code blocks (batch 12, package P4). Languages and highlighting for the CodeMirror feature,
// the language picker, fence-then-Enter, and the keys that stay inside a block. See
// docs/CONTRACT.md batch 12 "Code blocks".
//
// Exports read by extensions.js: plugins(ctx, o), featureConfig(o), registerCommands(api).

export function plugins() { return []; }
export function featureConfig() { return {}; }
export function registerCommands() {}
