// The editor's context menu (batch 12, package P3): right-click, Shift+F10 and the Menu key in
// the body. Built on shell/dialog.js `contextMenu`. See docs/CONTRACT.md batch 12.
//
// Exports read by extensions.js: plugins(ctx, o), registerCommands(api).

export function plugins() { return []; }
export function registerCommands() {}
