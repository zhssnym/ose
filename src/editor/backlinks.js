// Backlinks (batch 12, package P7): the "linked from" list under the page body, fed by
// lib/links.js findInbound. See docs/CONTRACT.md batch 12 "Links".
//
// Exports read by extensions.js: registerCommands(api).

export function registerCommands() {}
