// `[[` (batch 12, package P7): typing two brackets opens the page picker inline and inserts a
// markdown link to the chosen page (or creates it). Never writes wiki-link syntax. See
// docs/CONTRACT.md batch 12 "Links".
//
// Exports read by extensions.js: plugins(ctx, o).

export function plugins() { return []; }
