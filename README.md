ose is essentially Obsidian-like, but with custom GUI views generated 
directly from your plain-text/Markdown files. Your files remain the 
source of truth and can be read or edited by both humans and AI agents, 
while ose parses them to build a graphical interface on top, without 
requiring a database, index, sync, or proprietary format. For more 
information, see `docs/`.
